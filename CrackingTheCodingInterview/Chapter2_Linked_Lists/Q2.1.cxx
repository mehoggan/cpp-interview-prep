/* 
 * Q2.1: Remove Dups: Write code to remove duplicates from an unsorted linked
 * list. How would you solve this problem if a temporary buffer is not
 * allowed?
 */

#include "./include/linked_list.h"

#include <cassert>
#include <cstdlib>
#include <iostream>

int main(int, char *[])
{
  LinkedList list;
  list.insert(5);
  // LinkedList::Iterator head = list.begin();
  // assert(not head);
  std::cout << "Good bye cruel world." << std::endl;
  return EXIT_SUCCESS; 
}
